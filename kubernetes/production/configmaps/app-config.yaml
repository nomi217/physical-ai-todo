apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: production
data:
  # Frontend URL (update with your domain or LoadBalancer IP)
  FRONTEND_URL: "http://161-35-250-151.nip.io"

  # Backend API URL
  BACKEND_URL: "http://todo-backend:8000"

  # Environment
  NODE_ENV: "production"
  LOG_LEVEL: "info"

  # CORS origins (update with your domain)
  CORS_ORIGINS: "http://161-35-250-151.nip.io,https://your-domain.com"

  # Email configuration
  FROM_EMAIL: "onboarding@resend.dev"
